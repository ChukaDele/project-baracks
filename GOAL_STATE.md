# Goal state

## Active outcome

Install and production-validate exactly seven owner-supplied Brand OS skill packages through Major's canonical Skills Library lifecycle without changing unrelated skills or creating duplicate trigger surfaces.

## Acceptance evidence

- The seven source `.skill` archives remain byte-for-byte under `package/brand-os-input`; their SHA-256 hashes and truthful owner-supplied licence status are locked in `package/source-ledger.json`.
- The seven unchanged extracted packages are registered in registry version 18 with one resolver fixture each and specialist-specific trigger boundaries. Broad brand requests route to `brand-os`; approved-positioning requests route to their owning specialist; founder authority routes to `personal-brand-authority`; website/product art direction and SEO remain with their existing Major owners.
- Major's canonical validator, focused resolver/hot-sync tests, exact-SHA hot activation, rollback to the recorded predecessor, reactivation, and a representative fresh resolver operation pass.
- The release branch remains isolated and unrelated skills/projects are unchanged.

## Critical path

Fast deterministic validation → exact commit → production hot-bundle activation → rollback proof → exact-bundle reactivation → representative fresh resolution → independent grading by the parent coordinator.

## Decisions

- INTEGRATE the supplied router/specialist set unchanged; Major owns routing and lifecycle metadata only.
- Keep `brand-os` limited to complete programmes. Route positioning, naming, verbal identity, identity design, red-team review and founder authority directly to their specialists.
- Preserve `design-direction-and-taste` as the website/product art-direction authority.
- Reuse the existing registry, installer, bundle hashing, atomic pointer activation and recorded-predecessor rollback path; add no new subsystem.

## Current status

The seven packages, provenance lock, registry entries, resolver fixtures, reconciliation records, catalog boundaries, exact-seven hash validator and exclusive routing group are BUILT in the isolated canonical worktree. Canonical validation reports 94 internal skills and one reusable asset. Focused positive, negative, near-neighbour, resolver, hot-sync and rollback tests pass 113/113; typecheck, `src` lint, repository-wide formatting and diff checks pass.

The real lifecycle code activated bundle `91bc371e66ba0c57780396c3a780c4ba306240043356476eab5705a23d33aa29` from the exact committed tree in an admitted runtime with registry version 18 and 94 skills. Ten representative routes proved broad `brand-os`, approved-positioning specialist, founder-authority, product art-direction and SEO ownership with no Brand OS near-neighbour leakage. Rollback restored predecessor `8adaecacb4d4abbb0cf0a66a6c3f9a31d6d0067a2fad83c1874d48299117e0bd` and excluded `brand-os`; exact reactivation restored fresh broad-brand resolution.

## Current bottleneck

Production activation under `/Users/chukwuka/.major/skill-bundles` is the only remaining gate. The supported clean-checkout installer validated the candidate, then Seatbelt denied creation of its staging directory; the existing production `current` pointer remained unchanged. A host-authorized run must sync the final exact SHA and repeat the recorded predecessor rollback/reactivation proof before READY can be claimed.
