# Goal state

## Active outcome

Optimize Major v0.5.3 in place with measured progressive skill and client disclosure, bounded context packs, evidence-qualified history and learning, deterministic-first execution, safe stale-resource reclaim, and selected GBrain donor adaptations while preserving GBrain, existing client paths, containment, history, and thin runtime ownership.

## Acceptance evidence

- `major_context` accepts explicit detail, section, and byte budgets and returns source-qualified data without creating another memory store. Zero-argument callers retain the v1 serialization.
- The canonical resolver exposes HOT, ACTIVE, and DORMANT skill disclosure with bounded manifest/body bytes and estimated token metrics. Coordinator and routed CLI paths use the same contract.
- Resource leases retain historical behavior while adding TTL, grace, reclaimAfter, task identity, resource identity, fencing identity, live-PID protection, atomic reclaim, supported repair, and reclaim telemetry.
- Historical run receipts, performance history, incidents, outcomes, and validated learning remain durable. No history deletion or second memory store was introduced.
- GBrain donor review is provenance-backed. Seven independently authored Major workflow skills route durable meaning through existing GBrain interfaces; donor page-writing, archival storage, provider-specific behavior, queues and runtimes remain outside Major.

## Critical-path dependencies

- Context-pack projection → repository integration owner → existing `MajorDashboard` input and MCP output.
- Independent grading → parent coordinator → frozen candidate and evidence below.

## Ownership and interfaces

- Context pack → Major core → `MajorDashboard` in, bounded JSON projection out.
- Client exposure → MCP adapter → unchanged `major_context` tool name with optional disclosure controls.

## Current status

The implementation is committed at exact SHA `116f7e5` (`feat: optimize Major context and learning loop`). The independent exact-head review and canonical release-gate evidence remain current and passed. The branch has not yet been pushed or merged, and the release has not been installed. Installed-binary behavior, provider authentication, and localhost UI proof remain unclaimed.

## Current bottleneck

Delivery of approved SHA `116f7e5`: push and normal merge, exact-SHA supported installation, then thin localhost-only UI and installed CLI behavior proof.

## Decisions and discoveries

- WRAP the existing dashboard instead of adding retrieval or memory infrastructure; GBrain and persisted run insights remain authoritative.
- BORROW progressive disclosure and deterministic budget enforcement as a pure projection; omit whole low-priority sections before exceeding the caller budget.
- KEEP the existing reachability-based cleanup and DSH/client execution paths unchanged.
- Complete the lease identity contract with backward-compatible `taskId`, `resourceId`, and `fencingToken` fields. Worker runs bind these to the admitted goal when available.
- Merge only evidence-gated donor principles. Do not promote a one-off measurement into durable policy.
- ADD independently authored `skill-harvest`, `knowledge-ingest`, `strategic-reading`, `idea-lineage`, `concept-synthesis`, `research-compendium` and `academic-verify` workflows. MERGE cross-modal, functional-area routing, deterministic-execution and optimizer principles into existing Major authority; GBrain remains the durable meaning/provenance store.

## Current repair evidence

- Final independent same-provider Codex exact-head review after repair: **APPROVED**, with no P0/P1/P2/P3 findings. The prior final reviewer also verified 135/135 focused tests.
- Canonical `bash scripts/validate-major-release.sh` passed after the minimal repair: 87 internal skills and 1 reusable asset; Major and stability validators; provider CLI contracts; install transaction validation; repository-wide Prettier; `src` ESLint; typecheck; production build; ordinary tests 104 files/886 tests; resource tests 7 files/139 tests with 5 expected unprovisioned-worker skips; immutable runtime snapshot smoke; and canonical release validation.
- The affected repair suites pass 30/30.
- This remains current exact-head release evidence for committed SHA `116f7e5`, not installation or runtime behavior proof. The branch has not been pushed or merged, the release has not been installed, and installed-binary behavior, provider authentication, and localhost UI remain unproven.

## Historical evidence (pre-repair; not current exact-head proof)

- Progressive-disclosure measurement: full registry/body estimate 200,330 bytes and 50,083 estimated tokens; current task disclosed 19,465 bytes and 4,867 estimated tokens. The resolver uses an 8,000-byte manifest budget, 32,000-byte aggregate body budget, and 12,000-byte per-body budget.
- A direct acceptance probe produced a summary pack within the requested 2,000-byte ceiling containing three requested sections, three memory items, and derived skill evidence.
- Historical `git diff --check` pass.
- Focused resolver/sync tests pass 15/15; coordinator, CLI, MCP, and resource tests pass 63/63 before the lease-contract extension.
- Resource tests pass 12/12 after the lease-contract extension. Worker lease lifecycle identity/heartbeat, staged validation, and fencing checks pass.
- Major skill and stability validators pass. Typecheck, full `src` lint, and repository-wide Prettier pass.
- Historical full host-access ordinary suite: 102/102 files and 863/863 tests. This predates the repair and is not current exact-head proof. The historical isolated workstation-app suite passed 13/13, and the serial resource suite passed 111 tests with 5 expected unprovisioned-worker skips.
- Historical pre-repair release-gate receipt: Major skill/stability/release validators, typecheck, lint, repository-wide Prettier, diff checks, production build, and host-backed release validation passed. None is a current release attestation for this repaired checkout.
- Donor disposition record covers the requested candidates and exact aliases, including `skill-harvest`/`skillpack-harvest`, `knowledge-ingest`/`ingest`, and `functional-area-resolver`. No donor checkout, credential, package, or external system was modified.
- All requested donor rows now have `ADD` or `MERGE` dispositions; no requested `SKIP` or `REFERENCE_ONLY` remains. The registry/tree validator reports 87 reachable internal skills, preserving the prior 80 and adding seven.
- Isolated resolver and near-neighbour evaluation passes 77/77; hot skill sync passes 4/4 and reports all 87 internal skills in registry version 17.
- Pure knowledge semantics tests pass 4/4 for canonical aliases, duplicate/related/contradictory classification, time-qualified truth, explicit supersession and deterministic-versus-semantic brain-doctor findings.
- Typecheck, focused lint, formatting, skill validation and `git diff --check` pass for this increment. Repository-local binaries were used because the sandbox denies the system Corepack launcher.
- The source `resource reclaim` command and lease telemetry are validated by source tests. The installed v0.5.3 CLI was checked separately and still reports `resource` as an unknown command, so this increment is not installed or behaviorally proven through the installed binary.

## Next executable action

Push SHA `116f7e5` for normal merge, install the merged exact SHA through the supported transaction, then run thin localhost-only UI and installed CLI behavior proof.

## Owner-only gate

The owner must perform the push/normal merge and supported exact-SHA installation, authorize any provider authentication, and run the thin localhost-only UI listener plus installed CLI behavior proof. None of those remaining owner gates is currently claimed complete.
